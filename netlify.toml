[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: every route is served by index.html so client-side routing works.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
